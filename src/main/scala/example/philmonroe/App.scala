package example.philmonroe

import com.massrelevance.dropwizard.ScalaApplication
import io.dropwizard.setup.{Environment, Bootstrap}

/**
 * Created by philip.monroe on 6/2/14.
 */
object App extends ScalaApplication[Config] {
  override def initialize(bootstrap: Bootstrap[Config]): Unit = {

  }

  override def run(configuration: Config, environment: Environment): Unit = {

  }
}